import { GermanCostsPipe } from './german-costs.pipe';

describe('GermanCostsPipe', () => {
  it('create an instance', () => {
    const pipe = new GermanCostsPipe();
    expect(pipe).toBeTruthy();
  });
});
